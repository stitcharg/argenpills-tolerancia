export interface iDaysProps {
	days: number
}

export interface iResults {
	days: number,
	color: string,
	title: string,
	children: React.ReactNode
}

export interface iToleranciaProps {
	tolerancia: string,
	days: number
}